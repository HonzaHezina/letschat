Dockerfile
.dockerignore
node_modules
npm-debug.log
yarn-debug.log
yarn-error.log
.next
.git
.gitignore
README.md
# Add any other files/directories that are not needed for the build or runtime
# For example, local environment files if they are not used in the Docker build process.
# .env*.local (though these should not be copied by `COPY . .` if properly gitignored and not explicitly copied)
